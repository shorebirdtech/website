---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    new Intl.DateTimeFormat('en-US', {
      dateStyle: 'long',
    }).format(date)
  }
</time>
