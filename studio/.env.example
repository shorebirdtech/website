# Origins of the server-rendered preview deployments the Presentation tool
# loads, one per workspace. Baked in at `sanity build`/`sanity deploy`.
SANITY_STUDIO_PREVIEW_ORIGIN_DEV=
SANITY_STUDIO_PREVIEW_ORIGIN_PRODUCTION=
